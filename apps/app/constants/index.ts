export const PROTO = {
  SERVER_URL: "localhost:8787",
};

export const STATE = {
  TEST_DOCUMENT_ID: "test-document-id",
};

export const TRANSFORM = {
  SPEED_FACTOR: 2,
  MIN_WIDTH: 1,
  MIN_HEIGHT: 1,
  DEFAULT_AREA_OF_INTERACTION: 30,
  THROTTLE_AMOUNT: 10,
};

export const SELECT = {
  DELETION_THRESHOLD: 50,
};

export const CANVAS = {
  THROTTLE_DELAY: 300,
};

export const DOCUMENT = {
  THROTTLE_DELAY: 300,
};

export const DRAWING = {
  MIN_DIMENSION_SIZE: 50,
  DIMENSION_ADJUSTMENT: 20,
};
